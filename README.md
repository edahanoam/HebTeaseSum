## Quick Start

### Download the data

```bash
pip install -r requirements.txt

python download_data.py \
    --in=<PATH_TO_JSON_FILE> \
    --out=<OUTPUT_DIR> \
    --timeout=<MINUTES>
Choosing the input file (--in=)
